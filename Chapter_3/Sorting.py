colors = ['D', 'A', 'C']
colors.sort()
print(colors)

print(sorted(colors, reverse=True)) # When reversed True = decending
print(sorted(colors, reverse=False)) # When reversed False = ascending

print(len(colors)) # Number of Objects
print(len(colors) - 1 ) # Number of indexes